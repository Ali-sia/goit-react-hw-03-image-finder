import PropTypes from 'prop-types';

const Button = () => {
  return (
    <button className="xfbg" type="button">
      Load more
    </button>
  );
};

export default Button;

Button.propTypes = {};
